{
  "extends": "./.nuxt/tsconfig.json",
  "exclude": [
    "dist",
    "node_modules",
    "playground",
    // A standalone Nuxt app: its auto-imports live in its own .nuxt, which this
    // project's tsconfig cannot see.
    "test/fixtures",
  ]
}
