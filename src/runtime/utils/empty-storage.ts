// Stands in for `eponyme.storage.ts` when the host application declares none. The alias always
// resolves so server code can import it unconditionally; `null` is what says storage is off.
export default null
