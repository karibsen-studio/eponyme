🩹 Fixes

* Resolve `defineEponymeConfig`, `collection`, `form` and `defineEponymeVariables` from the built entry, so the auto-imports work in the published package (#19)

❤️ Contributors

* C.Nelhomme (`@D3ller`)